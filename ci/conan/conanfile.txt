[requires]
spdlog/1.5.0
gtest/1.10.0
#wiringpi/2.50@conan/stable

[generators]
cmake

[options]
# shared option set to True for shared library (.so) or False for static(.a)
spdlog:shared=True
gtest:shared=True

[imports]
# import functional libraries
# include, *.h -> ./../external/include/ @ root_package=spdlog
# lib, *.so* -> ./../build/lib

# import testing libraries
# include, *.h -> ./../tests/Google_tests/include/ @ root_package=gtest
# lib, *.so* -> ./../build/tests/Google_tests/lib @ root_package=gtest

# lib, *.so* -> ./../build/lib