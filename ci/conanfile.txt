[requires]
spdlog/1.5.0
gtest/1.10.0

[generators]
cmake

[options]
spdlog:shared=True
gtest:shared=True

[imports]
bin, *.dll -> ./../lib # Copies all dll files from packages bin folder to my "bin" folder
lib, *.dylib* -> ./../lib # Copies all dylib files from packages lib folder to my "bin" folder
lib, *.so* -> ./../lib # Copies all dylib files from packages lib folder to my "bin" folder